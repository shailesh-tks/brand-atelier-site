# Sanity — create a project at https://sanity.io/manage, then copy this file
# to .env.local and fill these in. Without them the site still builds and runs;
# the Proof section simply doesn't render and /studio shows setup instructions.
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
